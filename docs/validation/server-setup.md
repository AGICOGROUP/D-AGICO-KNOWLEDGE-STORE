# Windows 单目录安装验证

日期：2026-09-16。目标：新服务器拉取 Git 项目后，通过一个必填 `DataRoot` 参数初始化独立知识库；现有本机测试库不被迁移或覆盖。

## 已验证

- 在全新代码副本、全新中文数据目录，以独立 API／数据库端口运行真实安装入口。
- 自动下载并校验固定版本 uv、micromamba、WinSW；安装 Python 3.12.14、锁定生产依赖、PostgreSQL 16.15 和 pgvector 0.8.6。
- 初始化空数据库、权限身份和受保护凭证，下载向量及 OCR 模型；原文件、数据库、模型、配置放在数据目录。
- 实际注册三个 Windows 自动启动服务，分别以独立 `NT SERVICE` 虚拟账号运行；API 就绪检查通过。
- 使用全新合成 Word 与图片，经实际 HTTP 上传，后台服务完成解析、向量索引／OCR；Word 为 ready，图片为 partial 并保留 OCR 核对提示。原文件下载 SHA-256 一致。
- 经真实 MCP 客户端完成工具发现、文件名检索、内容检索及带出处读取；无凭证访问被拒绝。应用数据库角色不是超级用户，也无建库、建角色权限。
- 同一目录重复执行安装，访问凭证保持不变；三个服务手动停止后重新启动，继续处理全新文件。
- 两轮独立生成的合成文件均通过验证，数据库中四个片段具有向量，重启后的混合检索没有降级警告。验证结束已移除测试服务注册和监听端口，保留测试数据；原本机预览就绪检查仍通过。
- 修复并验证 Windows Python 版本别名、Hugging Face 模型缓存内部链接的权限处理；拒绝非预期链接及越界目标。
- 安装互斥锁覆盖代码和数据两个目录；直接运行初始化器也有排他锁，避免并发初始化互相停止数据库。
- 仓库发布审查未发现实际凭证、原始业务文件名、个人目录或二进制运行资料。采用干净初始提交，原开发历史保留在本地。

## 自动检查

全量 pytest：147 项通过、2 项按默认配置跳过（需显式启用的隔离进程／数据库重启测试）。其中新增初始化单元测试 4 项、Windows PowerShell 5.1 安装保护测试 7 项全部通过；独立服务 XML／身份／ACL／缓存链接脚本测试通过，Ruff 检查通过。实际 Windows 服务停止／启动另按上述真实安装流程验证。最终范围内 review 无剩余 Important／Critical 问题。

## 边界

本次是当前 Windows 主机上的隔离部署验证，没有重启整台电脑；已核对自动启动配置并实际停止／启动服务。没有据此宣称目标服务器、跨机器 HTTPS、Accio／钉钉入口或 200 Agent 容量已经验收。实际安装还需要管理员权限、Git、Windows 运行环境及首次联网下载。升级、跨机恢复按运维流程进行，不通过再次初始化代替。
